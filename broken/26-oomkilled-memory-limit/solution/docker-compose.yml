services:
  report:
    image: python:3.12-alpine
    command: ["python", "/job/report.py"]
    volumes:
      - ./job:/job:ro
    mem_limit: 96m
    memswap_limit: 96m      # no swap: the limit is a hard limit
    restart: "no"
