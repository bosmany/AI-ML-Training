# TODO (task 4): GitHub Actions workflow.
#   - on: push (main) and pull_request
#   - top-level least-privilege permissions
#   - job "test": checkout, setup-python (same version as your Dockerfile, cache: pip), install, pytest tests/health_check.py
#   - job "build": needs: test; builds the Docker image; pushes only when not a pull request
#   - pin every action (@v4 / commit SHA), timeout-minutes on every job, credentials only via ${{ secrets.* }}
