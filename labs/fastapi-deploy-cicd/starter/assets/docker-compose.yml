# TODO (task 3): compose file with two services.
#   db  : a pinned postgres image, healthcheck (pg_isready), password from ${POSTGRES_PASSWORD}
#   api : build: {context: .}, port "${API_PORT:-8000}:8000", healthcheck on /health,
#         depends_on: {db: {condition: service_healthy}}, DATABASE_URL built from ${POSTGRES_PASSWORD}
#   Both: restart policy. Secrets are variables, never literals.
