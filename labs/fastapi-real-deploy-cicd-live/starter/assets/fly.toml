# TODO (task 1): write fly.toml for this app. Build context is the repo root (reuse the Dockerfile
# from labs/fastapi-deploy-cicd - Fly builds it the same way `docker build` would).
#   - app = "<your-app-name>" (lowercase letters, digits, hyphens only) and primary_region = "<region>"
#   - [http_service] with internal_port matching the port the app listens on (8000), force_https = true
#   - at least one [[http_service.checks]] block: method "GET", path "/health", interval/timeout/grace_period set
#   - [[vm]] with a size (e.g. "shared-cpu-1x")
#   - never put secrets in an [env] table - those go in `flyctl secrets set` at deploy time
# Check yourself any time with:  python -m lab   (run from the starter/ folder)
