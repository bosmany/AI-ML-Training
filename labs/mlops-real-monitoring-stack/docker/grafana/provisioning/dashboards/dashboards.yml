# Tells Grafana to auto-load every dashboard JSON in /var/lib/grafana/dashboards on startup.
apiVersion: 1

providers:
  - name: fastapi-app
    orgId: 1
    folder: ""
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
