# Pre-configured Grafana datasource pointing at the real Prometheus container over the compose
# network - a human opening Grafana never has to click through the "add datasource" wizard.
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true
