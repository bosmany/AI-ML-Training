# Dependencies of scripts/check-notebooks.py and of what the fastapi/*.ipynb notebooks import
# (used by .github/workflows/notebooks.yml). The notebooks' own "!pip install" lines are
# satisfied by these, so the run is fast and reproducible.
nbclient>=0.10
nbformat>=5.10
ipykernel>=6.29
fastapi
uvicorn
httpx
sqlalchemy
aiosqlite
pydantic
pyjwt
bcrypt
python-multipart
pytest
